# Documentation Review Findings - 2026-05-05

This review follows the new source-of-truth rule: current code and merged GitHub PRs override older task-board, handoff, and baseline docs.

## Review scope

- `docs/agent/README.md`
- `docs/agent/05_TASK_BOARD.md`
- `docs/agent/PROJECT_FILE_INDEX.md`
- `docs/agent/references/PR_DERIVED_STATUS_2026-05-05.md`
- Recent merged backend PRs #15-#38
- Current `package.json`

## Current accepted facts from PRs and code

- `lian-platform-server` is the active backend/API/runtime repo.
- Backend repo bootstrap is no longer future planning; the backend repo exists and has its own checks.
- `npm run check` now includes backend structure, encoding, code-smell guard, context-doc guard, docs maintenance guard, generated automation-docs check, and route registry contract testing.
- `npm run verify` is the broad backend verification matrix.
- API routing now has `api-route-registry.js` and route-registry contract checks from PR #36.
- Backend docs inventory is generated by docs tooling from PR #37.
- Automation-first docs strategy landed in PR #38.

## Files with stale or risky interpretation

| File | Issue | Action |
|---|---|---|
| `docs/agent/05_TASK_BOARD.md` | Still contains older P0/P1 repo bootstrap and stabilization wording from before backend guardrail PRs #35-#38. | Do not use as first source of truth. Compare with `PR_DERIVED_STATUS_2026-05-05.md` and current `package.json`. |
| `docs/agent/PROJECT_FILE_INDEX.md` | Source-of-truth order still puts latest handoff before PR-derived status; references list 2026-05-04 files as current. | Update in a future cleanup PR or treat this findings file as an override. |
| `docs/agent/references/RECENT_WORK_HANDOFF_2026-05-04.md` | Historical handoff only; does not know about backend PRs #35-#38. | Keep for history; do not use for current backend checks. |
| `repo-split-manifest.json` | May preserve transition-era split metadata. | Treat as historical unless current PR or code confirms it. |

## Recommended next doc cleanup

1. Add a compact `Current PR-Derived Status` block at the top of `05_TASK_BOARD.md`.
2. Update `PROJECT_FILE_INDEX.md` source-of-truth order to include merged PRs and `PR_DERIVED_STATUS_2026-05-05.md` before handoffs.
3. Update `PROJECT_FILE_INDEX.md` references to include 2026-05-05 PR-derived status and mark 2026-05-04 references as historical.
4. Remove or supersede wording that describes backend repo bootstrap as future planning.
5. Keep feed-scoring follow-up narrow: import extracted helpers into `feed-service.js` in a separate PR if still desired.

## Safe implementation rule

Before starting any backend task, check:

1. current `package.json` scripts;
2. latest merged PRs;
3. `PR_DERIVED_STATUS_2026-05-05.md`;
4. current source files;
5. only then read older task docs and handoffs.
