# Task Board Override - 2026-05-05

This file overrides stale active-status readings in `docs/agent/05_TASK_BOARD.md`.

Use this file before the long task board. The task board remains useful as history, but it contains old P0/P1 statements that predate the merged backend guardrail and automation PR burst.

## Source of truth

1. Current code on `main`.
2. Merged GitHub PRs.
3. `docs/agent/references/PR_DERIVED_STATUS_2026-05-05.md`.
4. This override file.
5. `docs/agent/05_TASK_BOARD.md` for historical context only.

## Backend current state

- Backend repo: `taoyu051818-sys/lian-platform-server`.
- Frontend repo: `taoyu051818-sys/lian-mobile-web`.
- Historical full-stack repo: `taoyu051818-sys/lian-mobile-web-full`.
- Backend repo bootstrap is done; backend work happens in this repo.
- `npm run check` includes structure, encoding, smell guard, context-doc guard, docs maintenance guard, generated docs check, and route registry tests.
- `npm run verify` is the broad backend verification matrix.
- Route dispatch is protected by `api-route-registry.js` and `scripts/test-route-registry.js`.
- Docs inventory is generated by `npm run docs:list`.

## Superseded task-board readings

Do not treat these old readings as current without PR/code recheck:

- backend repo bootstrap as future planning;
- `lian-mobile-web-full` as active backend source;
- old P0/P1 split-gating language that predates PRs #35-#38;
- manual docs indexes as current source of truth;
- route ownership described only through the old monolithic router.

## Current backend workstreams from merged PRs

| Area | Current status |
|---|---|
| CI | Backend CI exists. |
| Verification | `npm run verify` unifies backend verification. |
| Code quality | Code-smell guard is part of `npm run check`. |
| Context sync | Context-doc guard is part of `npm run check`. |
| Docs maintenance | Generated docs/listing and maintenance checks exist. |
| Routing | `api-route-registry.js` and route-registry tests guard routing. |
| Static fallback | Backend-only fallback handles missing frontend assets cleanly. |
| Config | Config parsing is centralized in `config-schema.js`. |
| Feed scoring | Pure scoring helpers and tests are extracted; production wiring remains a narrow follow-up if needed. |

## Current backend follow-ups

These are the safest follow-ups to use for new work:

1. Pull latest `main` and run `npm run check`.
2. Run `npm run verify` when environment dependencies are available.
3. If continuing feed cleanup, wire extracted `feed-scoring.js` helpers into `feed-service.js` in a separate narrow PR.
4. Keep docs inventory generated; do not restore hand-maintained docs lists.
5. Keep route changes synchronized with `api-route-registry.js` and route-registry tests.

## Rule for future edits

If `05_TASK_BOARD.md` and this file disagree, use this file and current PRs for active planning. Keep the long task board as historical audit context until a dedicated cleanup PR rewrites it safely.
